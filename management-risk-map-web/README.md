# Management Risk Map
Independent web-only module. Uses the existing passwordless login and `/api/v1/auth/me`. Only `management.demo` is accepted. Data is synthetic aggregate GeoJSON; no member PII or exact home GPS is included.
