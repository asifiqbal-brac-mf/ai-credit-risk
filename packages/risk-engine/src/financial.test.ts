import {describe,expect,it} from 'vitest'; import {calculateFinancial} from './index';
describe('financial assessment',()=>{it('calculates surplus and installment deterministically',()=>{const r=calculateFinancial({monthlyIncome:30000,monthlyExpense:18000,externalDebt:2000,proposedAmount:100000,proposedTermMonths:12}); expect(r.monthlySurplus).toBe(10000); expect(r.proposedInstallment).toBeCloseTo(8333.33,2); expect(r.affordable).toBe(true);}); it('rejects non-positive terms',()=>expect(()=>calculateFinancial({monthlyIncome:1,monthlyExpense:0,externalDebt:0,proposedAmount:1,proposedTermMonths:0})).toThrow());});
